services:
  eng-n-011:
    build: .
    ports:
      - "18114:8080"
    environment:
      ADDR: ":8080"
      PORT: "8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 5s
      timeout: 3s
      retries: 10
