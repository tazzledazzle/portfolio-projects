{
  "name": "bazel-multibuild-frontend",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "pnpm": {
    "onlyBuiltDependencies": []
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --input-type=module -e \"import { greet } from './dist/index.js'; if (greet('Bazel') !== 'Hello, Bazel!') throw new Error('fail')\""
  },
  "devDependencies": {
    "typescript": "5.4.5"
  }
}
