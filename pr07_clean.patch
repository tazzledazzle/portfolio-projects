diff --git a/tools/run b/tools/run
new file mode 100755
index 0000000..111aaa1
--- /dev/null
+++ b/tools/run
@@ -0,0 +1,50 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+if [ $# -lt 1 ]; then
+  echo "Usage: $0 <project-id> [args]"
+  exit 1
+fi
+PROJECT=$1
+shift
+
+CONFIG="tools/run.yaml"
+CMD=$(yq e ".projects[] | select(.id==\"$PROJECT\").command" "$CONFIG")
+
+if [ -z "$CMD" ]; then
+  echo "No entry for project '$PROJECT' in $CONFIG"
+  exit 1
+fi
+
+echo "Running '$PROJECT' → $CMD $*"
+eval "$CMD $*"
diff --git a/tools/run.yaml b/tools/run.yaml
new file mode 100644
index 0000000..222bbb2
--- /dev/null
+++ b/tools/run.yaml
@@ -0,0 +1,20 @@
+projects:
+  - id: ws-chat-fast
+    command: "cd ws-chat-fast && docker compose up --build"
+  - id: projgen
+    command: "cd projgen && python -m projgen"
+  - id: rabbit-mq
+    command: "cd rabbit-mq && docker compose up"
+  - id: online-bookstore
+    command: "cd online-bookstore && docker compose up"
+  - id: bazel-multibuild
+    command: "cd bazel-multibuild && bazel build //..."
