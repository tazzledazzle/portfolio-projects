---
phase: 01-fix-p0-p1-issues
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - bazel-multibuild/java/MODULE.bazel
autonomous: true
requirements:
  - P0-03

must_haves:
  truths:
    - "cd bazel-multibuild/java && bazel test //... exits 0"
    - "bazel-multibuild/java/MODULE.bazel does not reference jcenter.bintray.com"
    - "bazel-multibuild/java/BUILD has no uncommented oci_image/tar/container_structure_test references that would cause analysis errors"
  artifacts:
    - path: "bazel-multibuild/java/MODULE.bazel"
      provides: "Bazel module dependency declarations"
  key_links:
    - from: "bazel-multibuild/java/BUILD"
      to: "bazel-multibuild/java/MODULE.bazel"
      via: "load() statements"
      pattern: "bazel_dep"
---

<objective>
Verify that `cd bazel-multibuild/java && bazel test //...` reaches test execution with no BUILD analysis errors, and remove the deprecated jcenter.bintray.com Maven repository from MODULE.bazel to prevent future resolution failures.

The container targets (`oci_image`, `tar`, `container_structure_test`) are already commented out in BUILD. The java and JUnit tests already pass. This plan:
1. Runs the verification to confirm current passing state
2. Removes the jcenter.bintray.com repository URL (deprecated/unreachable) from MODULE.bazel's maven.install artifacts list
3. Re-runs `bazel test //...` to confirm no regression

Purpose: Eliminating jcenter prevents non-reproducible builds as jcenter is shut down. The CONCERNS.md flagged this as a dependency-at-risk item. P0-03 requires no BUILD analysis errors and test execution reachability.
Output: `bazel test //...` from `bazel-multibuild/java/` passes with jcenter removed from MODULE.bazel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify bazel test passes and remove deprecated jcenter repository</name>
  <files>bazel-multibuild/java/MODULE.bazel</files>
  <read_first>
    - bazel-multibuild/java/MODULE.bazel — read the full file before modifying; note the maven.install repositories list
    - bazel-multibuild/java/BUILD — confirm oci_image/tar/container_structure_test targets are commented out and no uncommented references remain
  </read_first>
  <action>
    Step 1: Run `cd bazel-multibuild/java && bazel test //...` to confirm the current baseline passes. If it fails with an analysis error, read the error message and resolve it (the container targets must remain commented out).

    Step 2: Edit `bazel-multibuild/java/MODULE.bazel` to remove the `"https://jcenter.bintray.com/"` entry from the `maven.install` repositories list inside the `maven = use_extension(...)` block.

    The repositories list currently contains:
    - "http://uk.maven.org/maven2"
    - "https://repo.maven.apache.org/maven2"
    - "https://jcenter.bintray.com/"

    After the edit it must contain only:
    - "https://repo.maven.apache.org/maven2"

    Remove the `"http://uk.maven.org/maven2"` entry as well (HTTP, not HTTPS — security risk). The canonical central Maven repository at `https://repo.maven.apache.org/maven2` is sufficient for guava and junit.

    Step 3: Run `cd bazel-multibuild/java && bazel test //...` again to confirm no regression. The test `//:myproject_test` must PASS.

    Do NOT uncomment any oci_image, tar, or container_structure_test targets in BUILD.
  </action>
  <verify>
    <automated>cd bazel-multibuild/java && bazel test //... 2>&1 | grep -E "PASSED|Build completed successfully"</automated>
    Expected: output includes "//:myproject_test  PASSED" and "Build completed successfully"
  </verify>
  <acceptance_criteria>
    - `grep "jcenter.bintray.com" bazel-multibuild/java/MODULE.bazel` returns no output (exit code 1)
    - `grep "http://" bazel-multibuild/java/MODULE.bazel` returns no output (exit code 1, no plain HTTP URLs)
    - `cd bazel-multibuild/java && bazel test //...` exits 0
    - The bazel output includes "1 test passes" or "//:myproject_test  PASSED"
    - The bazel output includes "Build completed successfully"
    - No oci_image, tar, or container_structure_test symbols appear uncommented in BUILD
  </acceptance_criteria>
  <done>`cd bazel-multibuild/java && bazel test //...` exits 0 with myproject_test PASSED. jcenter.bintray.com and the HTTP Maven URL are removed from MODULE.bazel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MODULE.bazel → Maven repositories | Bazel fetches JARs from declared repository URLs at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | jcenter.bintray.com Maven fetch | mitigate | Remove jcenter URL from MODULE.bazel; jcenter is shut down and could be re-registered by a malicious actor |
| T-03-02 | Tampering | http://uk.maven.org/maven2 (plain HTTP) | mitigate | Remove plain HTTP Maven URL; use only https://repo.maven.apache.org/maven2 |
| T-03-03 | Denial of Service | Bazel analysis failure on uncommented OCI targets | accept | Container targets remain commented out; no rules_oci/rules_pkg/container_structure_test deps in MODULE.bazel |
</threat_model>

<verification>
From repo root:
```
cd bazel-multibuild/java && bazel test //...
```
Exit 0. Output must contain:
- "Build completed successfully"
- "//:myproject_test  PASSED"

Also:
```
grep "jcenter" bazel-multibuild/java/MODULE.bazel
```
Must return no output (exit 1).
</verification>

<success_criteria>
- `cd bazel-multibuild/java && bazel test //...` exits 0 with 1 test passing
- No jcenter.bintray.com URL in MODULE.bazel
- No plain HTTP Maven URLs in MODULE.bazel
- BUILD file unchanged (container targets still commented out)
</success_criteria>

<output>
Create `.planning/phases/01-fix-p0-p1-issues/01-03-SUMMARY.md` when done.
</output>
