---
phase: 01-fix-p0-p1-issues
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - dev-env/onboarding-automation-cli/settings.gradle.kts
  - dev-env/onboarding-automation-cli/build.gradle.kts
autonomous: true
requirements:
  - P1-05

must_haves:
  truths:
    - "cd dev-env/onboarding-automation-cli && ./gradlew test exits 0"
    - "dev-env/onboarding-automation-cli/settings.gradle.kts exists with rootProject.name"
    - "dev-env/onboarding-automation-cli/build.gradle.kts has kotlin(jvm) plugin and dependencies block"
  artifacts:
    - path: "dev-env/onboarding-automation-cli/settings.gradle.kts"
      provides: "Standalone Gradle settings isolating the subproject from root build"
      contains: "rootProject.name"
    - path: "dev-env/onboarding-automation-cli/build.gradle.kts"
      provides: "Kotlin JVM build configuration with application plugin and dependencies"
      contains: "kotlin(\"jvm\")"
  key_links:
    - from: "dev-env/onboarding-automation-cli/settings.gradle.kts"
      to: "dev-env/onboarding-automation-cli/build.gradle.kts"
      via: "Gradle settings discovery"
      pattern: "rootProject.name"
    - from: "dev-env/onboarding-automation-cli/build.gradle.kts"
      to: "dev-env/onboarding-automation-cli/src/main/kotlin/com/company/onboarding/Main.kt"
      via: "kotlin JVM compilation"
      pattern: "kotlin(\"jvm\")"
---

<objective>
Fix `cd dev-env/onboarding-automation-cli && ./gradlew test` by:
1. Creating `settings.gradle.kts` so the subproject runs as a standalone Gradle build (not picked up by the root `settings.gradle.kts`)
2. Fleshing out `build.gradle.kts` with the Kotlin JVM plugin, application plugin, and stdlib dependency so `Main.kt` compiles and test compilation succeeds

Currently, running `./gradlew` from `dev-env/onboarding-automation-cli/` fails because:
- The root `settings.gradle.kts` includes "onboarding-automation-cli" as a project, but the path is not at root level — Gradle finds the root settings file and rejects the invocation
- Even if it ran, `build.gradle.kts` has empty `plugins {}` so no Kotlin compilation occurs
- There are no test sources; `./gradlew test` should succeed vacuously (test task runs with no test classes)

Purpose: P1-05 requires `./gradlew test` to resolve dependencies and run. The source exists (Main.kt, SetupCommand.kt, etc.) but the build cannot compile without the Kotlin JVM plugin.
Output: `cd dev-env/onboarding-automation-cli && ./gradlew test` exits 0. Dependencies resolve from mavenCentral(). No test failures (no test sources exist — empty test run is acceptable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create settings.gradle.kts for standalone subproject isolation</name>
  <files>dev-env/onboarding-automation-cli/settings.gradle.kts</files>
  <read_first>
    - dev-env/onboarding-automation-cli/build.gradle.kts — read to understand current (empty) plugin/dep declarations
    - dev-env/onboarding-automation-cli/gradle/wrapper/gradle-wrapper.properties — confirm Gradle version (8.10.2)
    - settings.gradle.kts (root) — read to understand that it currently has `include("onboarding-automation-cli")` as a stray top-level include (the actual path is dev-env/onboarding-automation-cli/, not at root)
    - rabbit-mq/settings.gradle.kts or modular-jvm-build/settings.gradle.kts — use as reference for standalone subproject settings file pattern
  </read_first>
  <action>
    Create `dev-env/onboarding-automation-cli/settings.gradle.kts` with the following content:

    rootProject.name = "onboarding-automation-cli"

    This single-line settings file is sufficient to declare the subproject as a standalone Gradle build. With this file present, running `./gradlew` from within `dev-env/onboarding-automation-cli/` will use this settings file instead of walking up to find the root `settings.gradle.kts`.

    Do NOT include any `pluginManagement {}` block — keep the file minimal. The wrapper at `gradle/wrapper/gradle-wrapper.properties` (Gradle 8.10.2) is already in place.
  </action>
  <verify>
    <automated>test -f dev-env/onboarding-automation-cli/settings.gradle.kts && grep "rootProject.name" dev-env/onboarding-automation-cli/settings.gradle.kts</automated>
    Expected: grep returns a match containing "onboarding-automation-cli"
  </verify>
  <acceptance_criteria>
    - File `dev-env/onboarding-automation-cli/settings.gradle.kts` exists
    - `grep "rootProject.name" dev-env/onboarding-automation-cli/settings.gradle.kts` returns `rootProject.name = "onboarding-automation-cli"`
    - File contains exactly 1 non-empty line (minimal settings file)
  </acceptance_criteria>
  <done>dev-env/onboarding-automation-cli/settings.gradle.kts exists with rootProject.name = "onboarding-automation-cli".</done>
</task>

<task type="auto">
  <name>Task 2: Add Kotlin JVM plugin + dependencies to build.gradle.kts and verify ./gradlew test</name>
  <files>dev-env/onboarding-automation-cli/build.gradle.kts</files>
  <read_first>
    - dev-env/onboarding-automation-cli/build.gradle.kts — read the current empty file before replacing
    - dev-env/onboarding-automation-cli/src/main/kotlin/com/company/onboarding/Main.kt — read to confirm package name and that it compiles with stdlib only
    - dev-env/onboarding-automation-cli/gradle/wrapper/gradle-wrapper.properties — confirm Gradle 8.10.2 (Kotlin plugin version must be compatible: use 1.9.24 as declared in the project name from STACK.md)
    - c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/build.gradle.kts — use as reference for a sibling Kotlin JVM project in this repo
  </read_first>
  <action>
    Rewrite `dev-env/onboarding-automation-cli/build.gradle.kts` with a complete Kotlin JVM build configuration:

    - plugins block: `kotlin("jvm") version "1.9.24"` and `application`
    - group = "com.company.onboarding", version = "0.1.0"
    - repositories block: `mavenCentral()` (already present, keep it)
    - dependencies block:
      - `implementation(kotlin("stdlib"))`
      - `testImplementation(kotlin("test"))`
    - kotlin block: `jvmToolchain(17)` (use JDK 17 for consistency with the subproject; the root uses 23 but this is a standalone build)
    - tasks.test block: `useJUnitPlatform()`
    - application block: `mainClass.set("com.company.onboarding.MainKt")` (Kotlin top-level `fun main()` generates a `MainKt` class)

    After rewriting, run:
    `cd dev-env/onboarding-automation-cli && ./gradlew test --no-daemon 2>&1 | tail -20`

    Expected: Gradle downloads dependencies from mavenCentral(), compiles Main.kt and sibling sources, runs the test task (empty — no test sources), and exits 0.

    If JDK 17 is not available in the local environment and the build fails on toolchain resolution, change `jvmToolchain(17)` to `jvmToolchain(21)` or `jvmToolchain(23)` to match the available JDK. Choose the lowest version available.

    Do NOT add Clikt, PicoCLI, or any other CLI library — Main.kt only uses stdlib println.
  </action>
  <verify>
    <automated>cd dev-env/onboarding-automation-cli && ./gradlew test --no-daemon 2>&1 | grep -E "BUILD SUCCESSFUL|0 tests"</automated>
    Expected: output contains "BUILD SUCCESSFUL"
  </verify>
  <acceptance_criteria>
    - `grep "kotlin(\"jvm\")" dev-env/onboarding-automation-cli/build.gradle.kts` returns a match
    - `grep "mavenCentral" dev-env/onboarding-automation-cli/build.gradle.kts` returns a match
    - `grep "jvmToolchain" dev-env/onboarding-automation-cli/build.gradle.kts` returns a match
    - `cd dev-env/onboarding-automation-cli && ./gradlew test --no-daemon` exits 0
    - The ./gradlew output contains "BUILD SUCCESSFUL"
    - The ./gradlew output does NOT contain "no repositories are defined"
    - The ./gradlew output does NOT contain "BUILD FAILED"
  </acceptance_criteria>
  <done>`cd dev-env/onboarding-automation-cli && ./gradlew test --no-daemon` exits 0 with BUILD SUCCESSFUL. Kotlin stdlib resolves from mavenCentral().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gradle wrapper → mavenCentral | Dependency resolution fetches JARs over HTTPS from Maven Central |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Gradle dependency resolution | mitigate | mavenCentral() only (HTTPS); no custom repositories or HTTP sources |
| T-06-02 | Denial of Service | Root settings.gradle.kts co-opting standalone subproject | mitigate | settings.gradle.kts in the subproject dir takes precedence; Gradle uses nearest settings file |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan; Gradle wrapper is already committed |
</threat_model>

<verification>
```
cd dev-env/onboarding-automation-cli && ./gradlew test --no-daemon 2>&1 | tail -5
```
Must contain "BUILD SUCCESSFUL".

```
grep "rootProject.name" dev-env/onboarding-automation-cli/settings.gradle.kts
```
Must return a match.

```
grep "kotlin(\"jvm\")" dev-env/onboarding-automation-cli/build.gradle.kts
```
Must return a match.
</verification>

<success_criteria>
- dev-env/onboarding-automation-cli/settings.gradle.kts exists with rootProject.name = "onboarding-automation-cli"
- dev-env/onboarding-automation-cli/build.gradle.kts has kotlin("jvm") plugin, mavenCentral(), and stdlib dependency
- `cd dev-env/onboarding-automation-cli && ./gradlew test --no-daemon` exits 0 (BUILD SUCCESSFUL)
</success_criteria>

<output>
Create `.planning/phases/01-fix-p0-p1-issues/01-06-SUMMARY.md` when done.
</output>
