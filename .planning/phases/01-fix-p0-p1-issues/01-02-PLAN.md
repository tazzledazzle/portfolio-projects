---
phase: 01-fix-p0-p1-issues
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - online-bookstore/src/requirements.txt
  - online-bookstore/pyproject.toml
autonomous: true
requirements:
  - P0-02

must_haves:
  truths:
    - "cd online-bookstore && python3 -m pytest test/ -q exits 0"
    - "python-multipart is listed in online-bookstore/src/requirements.txt"
    - "online-bookstore/pyproject.toml has a valid [project] table"
  artifacts:
    - path: "online-bookstore/src/requirements.txt"
      provides: "Runtime dependency list including python-multipart"
      contains: "python-multipart"
    - path: "online-bookstore/pyproject.toml"
      provides: "Package metadata"
      contains: "[project]"
  key_links:
    - from: "online-bookstore/test/test_main.py"
      to: "online-bookstore/src/main.py"
      via: "sys.path.insert + from main import app"
      pattern: "sys.path.insert"
---

<objective>
Fix `cd online-bookstore && pytest` so it collects and passes all tests. The FastAPI imports in `src/main.py` are already correct. The failure is a missing `python-multipart` dependency (required by FastAPI OAuth2PasswordRequestForm) and an empty `pyproject.toml` that prevents `pip install -e .` from succeeding in the CI portfolio pytest script.

Purpose: The portfolio CI script (`scripts/run_portfolio_pytest.sh`) calls `pip install -e .` for directories with a `pyproject.toml`, then runs pytest. With an empty `pyproject.toml` the install silently fails, and without `python-multipart` in `requirements.txt`, FastAPI raises a RuntimeError during import.
Output: `cd online-bookstore && python3 -m pytest test/ -q` exits 0 with all tests collected and passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add python-multipart to online-bookstore requirements</name>
  <files>online-bookstore/src/requirements.txt</files>
  <read_first>
    - online-bookstore/src/requirements.txt — read current dependency list before modifying
    - online-bookstore/src/main.py — confirm it imports OAuth2PasswordRequestForm from fastapi.security (this triggers the python-multipart requirement)
    - online-bookstore/test/test_main.py — read the test file to understand what endpoints are exercised
  </read_first>
  <action>
    Append `python-multipart` to `online-bookstore/src/requirements.txt`.

    The current file contains: fastapi, uvicorn, pydantic, sqlalchemy, pytest, websockets.

    Add `python-multipart` and `httpx` (required by fastapi.testclient used in test_main.py). The final file must list all of: fastapi, uvicorn, pydantic, sqlalchemy, pytest, websockets, python-multipart, httpx.

    Do NOT pin versions — keep consistent with the existing unpinned style.
  </action>
  <verify>
    <automated>grep "python-multipart" online-bookstore/src/requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep "python-multipart" online-bookstore/src/requirements.txt` returns a match
    - `grep "httpx" online-bookstore/src/requirements.txt` returns a match
    - The file is not otherwise changed (same format, no pinned versions added)
  </acceptance_criteria>
  <done>online-bookstore/src/requirements.txt contains python-multipart and httpx entries.</done>
</task>

<task type="auto">
  <name>Task 2: Fill online-bookstore pyproject.toml and verify pytest passes</name>
  <files>online-bookstore/pyproject.toml</files>
  <read_first>
    - online-bookstore/pyproject.toml — confirm it is empty (0 bytes) before writing
    - online-bookstore/src/requirements.txt — read after Task 1 to confirm python-multipart is present
    - online-bookstore/src/main.py — confirm the src package name (bare `main` module in src/)
    - rest-api-test-demo/pyproject.toml — use as a reference pattern for sibling FastAPI project pyproject.toml structure
  </read_first>
  <action>
    Write a valid `[project]` table to `online-bookstore/pyproject.toml`. The project uses a bare `src/` layout where `main.py` is at `online-bookstore/src/main.py` and tests use `sys.path.insert` to add `src/` to the path — there is no package to install as an editable install.

    The pyproject.toml should define:
    - `[project]` with name = "online-bookstore", version = "0.1.0", requires-python = ">=3.10"
    - `dependencies` listing the runtime deps from requirements.txt (fastapi, uvicorn, pydantic, sqlalchemy, python-multipart)
    - `[build-system]` using setuptools

    After writing the file, install dependencies and run the tests to verify:
    1. `pip install -q python-multipart httpx` (install the missing runtime dep in the current environment)
    2. `cd online-bookstore && python3 -m pytest test/ -q`

    The tests must collect without errors and pass. If any test fails due to a missing CSV data file or import, read the error, trace the cause in src/, and fix only the minimum necessary (do not refactor tests or app logic).
  </action>
  <verify>
    <automated>cd online-bookstore && python3 -m pytest test/ -q 2>&1 | tail -5</automated>
    Expected: output ends with "N passed" (no collection errors, no test failures)
  </verify>
  <acceptance_criteria>
    - `online-bookstore/pyproject.toml` is non-empty (wc -c returns > 0)
    - `grep "\[project\]" online-bookstore/pyproject.toml` returns a match
    - `cd online-bookstore && python3 -m pytest test/ -q` exits 0
    - The pytest output contains "passed" with no "error" on the final summary line
    - No new source files were created (only pyproject.toml modified and requirements.txt already done in Task 1)
  </acceptance_criteria>
  <done>online-bookstore/pyproject.toml has a valid [project] table. `cd online-bookstore && python3 -m pytest test/ -q` exits 0 with all tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → FastAPI app | TestClient sends HTTP to the in-process app; no external network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | OAuth2PasswordRequestForm endpoint | accept | Test-only; fake token generator already present; no real auth in scope |
| T-02-02 | Information Disclosure | pyproject.toml dependency list | accept | No secrets in dependency metadata; public packages only |
</threat_model>

<verification>
```
cd online-bookstore && python3 -m pytest test/ -q
```
Exit code 0. Final line contains "passed" with no "error".

```
grep "python-multipart" online-bookstore/src/requirements.txt
```
Returns a match.

```
wc -c < online-bookstore/pyproject.toml
```
Returns a value greater than 0.
</verification>

<success_criteria>
- `cd online-bookstore && python3 -m pytest test/ -q` exits 0
- online-bookstore/src/requirements.txt lists python-multipart and httpx
- online-bookstore/pyproject.toml has non-empty [project] metadata
</success_criteria>

<output>
Create `.planning/phases/01-fix-p0-p1-issues/01-02-SUMMARY.md` when done.
</output>
