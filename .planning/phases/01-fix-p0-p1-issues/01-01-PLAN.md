---
phase: 01-fix-p0-p1-issues
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - P0-01

must_haves:
  truths:
    - "git ls-files | grep __pycache__ returns empty output"
    - "git ls-files | grep .pytest_cache returns empty output"
    - "Root .gitignore contains entries for __pycache__/, *.pyc, .pytest_cache/, .venv/, .mypy_cache/"
  artifacts:
    - path: ".gitignore"
      provides: "Python artifact exclusions"
      contains: "__pycache__"
  key_links:
    - from: ".gitignore"
      to: "git index"
      via: "git rm --cached on tracked artifacts"
      pattern: "__pycache__"
---

<objective>
Remove all committed Python bytecode and cache artifacts from git tracking. The root .gitignore already has the correct entries; this plan executes `git rm --cached` on the 22 tracked `__pycache__` files and any `.pytest_cache` files in the index, then verifies the index is clean.

Purpose: Tracked bytecode bloats clones, creates noisy diffs, and risks committing accidental secrets if a venv is ever placed in a tracked path. The `.gitignore` already covers these patterns — only `git rm --cached` is needed to remove the stale tracked entries.
Output: A clean git index with no tracked Python bytecode or cache artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove tracked Python artifacts from git index</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore — verify the Python section contains __pycache__/, *.py[cod], .pytest_cache/, .mypy_cache/, .venv/, venv/ before running git rm
  </read_first>
  <action>
    Run the following commands in order from the repo root:

    1. Confirm tracked artifacts exist: `git ls-files | grep -E "__pycache__|\.pytest_cache|\.venv"` — note the count.

    2. Remove all tracked __pycache__ entries: `git rm -r --cached --ignore-unmatch "**/__pycache__"` — this must be run for all matching paths.

    3. Remove all tracked .pytest_cache entries: `git rm -r --cached --ignore-unmatch "**/.pytest_cache"`

    4. Remove any tracked .venv entries (should be none, but verify): `git rm -r --cached --ignore-unmatch "**/.venv"`

    5. Verify .gitignore has entries for all removed artifact types. The Python section must include:
       - `__pycache__/`
       - `*.py[cod]`
       - `.pytest_cache/`
       - `.mypy_cache/`
       - `.venv/` and `venv/`

    If any of these entries are missing from .gitignore, add them to the `### Python ###` section of .gitignore before proceeding.

    Do NOT commit the changes — this plan only stages the removal and verifies the index. The user will commit separately.
  </action>
  <verify>
    <automated>git ls-files | grep -cE "__pycache__|\.pytest_cache|/\.venv/"</automated>
    Expected output: 0 (command returns exit code 1 with no matches, or outputs "0")
  </verify>
  <acceptance_criteria>
    - `git ls-files | grep "__pycache__"` returns no output (exit code 1)
    - `git ls-files | grep ".pytest_cache"` returns no output (exit code 1)
    - `git ls-files | grep ".venv"` returns no output (exit code 1)
    - `git diff --cached --name-only | grep -c "pyc"` returns a positive number (confirming staged deletions)
    - .gitignore contains the line `__pycache__/`
    - .gitignore contains the line `.pytest_cache/`
    - .gitignore contains `.venv/`
  </acceptance_criteria>
  <done>All tracked __pycache__, .pytest_cache, and .venv entries are removed from the git index (staged for deletion). .gitignore covers all these patterns. Zero tracked Python artifacts remain after `git rm --cached`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git index → committed history | Bytecode files could theoretically embed secrets if venv paths are used |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | projgen/.venv (if re-tracked) | mitigate | .gitignore entry for .venv/ prevents re-tracking; verified in acceptance criteria |
| T-01-02 | Tampering | __pycache__ .pyc files in git | accept | Bytecode is compiled output, not a direct security vector; removal eliminates noise and drift risk |
</threat_model>

<verification>
From repo root:
```
git ls-files | grep -E "__pycache__|\.pytest_cache" | wc -l
```
Must output 0.

Also verify .gitignore has not regressed:
```
grep "__pycache__" .gitignore
grep ".pytest_cache" .gitignore
```
Both must return a match.
</verification>

<success_criteria>
- `git ls-files | grep "__pycache__"` returns empty (exit 1)
- `git ls-files | grep ".pytest_cache"` returns empty (exit 1)
- .gitignore contains `__pycache__/`, `.pytest_cache/`, `.venv/`
- `git diff --cached` shows only deletions of .pyc files (no modified source files)
</success_criteria>

<output>
Create `.planning/phases/01-fix-p0-p1-issues/01-01-SUMMARY.md` when done.
</output>
