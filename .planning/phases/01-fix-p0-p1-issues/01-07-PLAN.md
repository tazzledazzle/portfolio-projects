---
phase: 01-fix-p0-p1-issues
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/build.gradle.kts
autonomous: true
requirements:
  - P1-06

must_haves:
  truths:
    - "cd c0de-quality-and-analysis/kotlin-custom-detekt-rules-library && ./gradlew test detekt exits 0"
    - "build.gradle.kts has detekt plugin with version string"
    - "CustomRulesTest.kt compiles and all test assertions pass"
    - "META-INF/services/io.gitlab.arturbosch.detekt.api.RuleSetProvider registers provider.CustomRuleSetProvider"
  artifacts:
    - path: "c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/build.gradle.kts"
      provides: "Detekt plugin with version declared so Gradle can resolve it"
      contains: "io.gitlab.arturbosch.detekt"
  key_links:
    - from: "c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/build.gradle.kts"
      to: "c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/src/main/kotlin/provider/CustomRuleSetProvider.kt"
      via: "detektPlugins(project) dependency"
      pattern: "detektPlugins"
    - from: "c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/src/main/resources/META-INF/services/io.gitlab.arturbosch.detekt.api.RuleSetProvider"
      to: "c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/src/main/kotlin/provider/CustomRuleSetProvider.kt"
      via: "Java ServiceLoader"
      pattern: "CustomRuleSetProvider"
---

<objective>
Fix `cd c0de-quality-and-analysis/kotlin-custom-detekt-rules-library && ./gradlew test detekt` by adding the missing version number to the detekt plugin declaration in `build.gradle.kts`.

The subproject has all required files: `build.gradle.kts`, `settings.gradle.kts`, `gradlew`, source rules (`CoroutineScopeNamingRule.kt`, `UncheckedPlatformTypeCastRule.kt`, `MissingTransactionalAnnotationRule.kt`), `CustomRuleSetProvider.kt`, service registration file, and `CustomRulesTest.kt`. The only failing element is:

```
id("io.gitlab.arturbosch.detekt")
```

This declaration has no version. Gradle requires a version for plugins not in the Gradle core namespace. Adding `version "1.23.6"` (matching the `compileOnly("io.gitlab.arturbosch.detekt:detekt-api:1.23.6")` dependency already declared) fixes the resolution failure.

Purpose: P1-06 requires `./gradlew test detekt` to compile the custom rules and run tests. The CI workflow at `c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/.github/workflows/ci.yml` also depends on this.
Output: `cd c0de-quality-and-analysis/kotlin-custom-detekt-rules-library && ./gradlew test detekt` exits 0 with all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add detekt plugin version to build.gradle.kts and verify ./gradlew test detekt</name>
  <files>c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/build.gradle.kts</files>
  <read_first>
    - c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/build.gradle.kts — read the full file; note that `id("io.gitlab.arturbosch.detekt")` has no version and `detekt-api:1.23.6` is in the compileOnly dep
    - c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/settings.gradle.kts — confirm rootProject.name = "kotlin-custom-detekt-rules-library"
    - c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/src/test/kotlin/rules/CustomRulesTest.kt — read the test file to understand what assertions will run
    - c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/src/main/kotlin/provider/CustomRuleSetProvider.kt — read to confirm it implements RuleSetProvider
    - c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/src/main/resources/META-INF/services/io.gitlab.arturbosch.detekt.api.RuleSetProvider — confirm it contains "provider.CustomRuleSetProvider"
  </read_first>
  <action>
    Edit `c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/build.gradle.kts` to add the version to the detekt plugin declaration.

    Change:
      `id("io.gitlab.arturbosch.detekt")`
    To:
      `id("io.gitlab.arturbosch.detekt") version "1.23.6"`

    This is the only change required. The version `1.23.6` matches `detekt-api:1.23.6` already declared in the compileOnly dependency.

    Also check that the `detekt {}` configuration block references `config/detekt-custom-rules.yml`. If that file does not exist, create `c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/config/detekt-custom-rules.yml` as an empty YAML file (`{}`). The detekt task will fail if the config file path is declared but the file is missing.

    After making the change, run:
    `cd c0de-quality-and-analysis/kotlin-custom-detekt-rules-library && ./gradlew test detekt --no-daemon 2>&1 | tail -20`

    If `./gradlew test` passes but `detekt` fails due to rule violations in the project's own source code, add the violating files to `detekt { source = ... }` exclusions OR add a `baseline.xml`. The custom rules should not flag their own minimal source. If they do, add `detekt { ignoreFailures = true }` only as a last resort and document the violation in SUMMARY.

    Do NOT change the kotlin version, detekt-api version, or test framework.
  </action>
  <verify>
    <automated>cd c0de-quality-and-analysis/kotlin-custom-detekt-rules-library && ./gradlew test detekt --no-daemon 2>&1 | grep "BUILD SUCCESSFUL"</automated>
    Expected: returns "BUILD SUCCESSFUL"
  </verify>
  <acceptance_criteria>
    - `grep 'id("io.gitlab.arturbosch.detekt") version' c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/build.gradle.kts` returns a match containing version "1.23.6"
    - `cd c0de-quality-and-analysis/kotlin-custom-detekt-rules-library && ./gradlew test --no-daemon` exits 0
    - `cd c0de-quality-and-analysis/kotlin-custom-detekt-rules-library && ./gradlew detekt --no-daemon` exits 0
    - The test output contains "BUILD SUCCESSFUL" for the test task
    - `grep "Plugin \[id: .io.gitlab.arturbosch.detekt.\] was not found" /tmp/detekt_out.txt` returns no match (no plugin-not-found error)
  </acceptance_criteria>
  <done>`cd c0de-quality-and-analysis/kotlin-custom-detekt-rules-library && ./gradlew test detekt --no-daemon` exits 0. BUILD SUCCESSFUL. Detekt plugin resolves via version "1.23.6".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gradle plugin portal → detekt plugin | Plugin resolved from plugins.gradle.org over HTTPS using declared version |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | io.gitlab.arturbosch.detekt plugin resolution | mitigate | Pin to version "1.23.6"; Gradle Plugin Portal resolves by exact version; no floating "latest" |
| T-07-02 | Tampering | CustomRuleSetProvider ServiceLoader registration | accept | Service file registers only the local provider class; no external provider injection |
| T-07-SC | Tampering | Gradle dependency fetch | accept | Only mavenCentral() and Gradle Plugin Portal; both HTTPS |
</threat_model>

<verification>
```
cd c0de-quality-and-analysis/kotlin-custom-detekt-rules-library && ./gradlew test detekt --no-daemon 2>&1 | tail -5
```
Must contain "BUILD SUCCESSFUL".

```
grep 'version "1.23.6"' c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/build.gradle.kts
```
Must return a match.

```
cat c0de-quality-and-analysis/kotlin-custom-detekt-rules-library/src/main/resources/META-INF/services/io.gitlab.arturbosch.detekt.api.RuleSetProvider
```
Must return "provider.CustomRuleSetProvider".
</verification>

<success_criteria>
- build.gradle.kts has `id("io.gitlab.arturbosch.detekt") version "1.23.6"`
- `./gradlew test --no-daemon` exits 0 (BUILD SUCCESSFUL)
- `./gradlew detekt --no-daemon` exits 0 (BUILD SUCCESSFUL)
- CustomRulesTest.kt tests all pass (no test failures)
- Service registration file contains provider.CustomRuleSetProvider
</success_criteria>

<output>
Create `.planning/phases/01-fix-p0-p1-issues/01-07-SUMMARY.md` when done.
</output>
