# Telemetry Event Schema

Each event represents one tool adoption funnel step:
- `view`: team saw launch surface (portal or docs)
- `install`: team installed plugin/tooling
- `run`: team executed the tool in real work

Dimensions:
- `team`
- `tool`
- `timestamp` (to be added in implementation)
