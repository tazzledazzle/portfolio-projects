---
phase: 01-foundation
plan: 01-01
type: execute
wave: 1
depends_on: []
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
must_haves:
  truths:
    - "`docker compose -f deploy/docker-compose.yml up -d` reaches healthy state for Temporal, Grafana, Prometheus, Loki, Tempo within 3 minutes on 8GB RAM machine."
    - "`./scripts/smoke.sh` exits 0 and prints PASS for each checked endpoint."
    - "GitHub Actions (or local `./gradlew build test`) succeeds on empty Kotlin modules."
  artifacts:
    - path: deploy/docker-compose.yml
    - path: scripts/smoke.sh
    - path: settings.gradle.kts
    - path: .github/workflows/ci.yml
---

<objective>
Establish **repository skeleton**, **Compose platform**, and **CI** so later phases only add behavior—not infrastructure glue from scratch.
</objective>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@docs/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gradle multi-module skeleton</name>
  <files>settings.gradle.kts, build.gradle.kts, worker/, workflows/, starter/</files>
  <behavior>
    - Kotlin JVM 21, Temporal SDK dependency declared (unused OK).
    - `:worker` depends on `:workflows`.
    - `:starter` application plugin with empty `main`.
    - `./gradlew build test` passes (placeholder test).
  </behavior>
  <verify><automated>./gradlew build test</automated></verify>
  <done>FOUND-03 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Docker Compose stack</name>
  <files>deploy/docker-compose.yml, deploy/prometheus/prometheus.yml, deploy/loki/loki-config.yml, deploy/tempo/tempo.yaml, deploy/grafana/provisioning/datasources/*.yml</files>
  <behavior>
    - Services: temporal, temporal-ui, prometheus, loki, promtail, tempo, grafana, otel-collector (minimal valid config).
    - Healthchecks on HTTP ports where applicable.
    - Document RAM recommendation in README (≥8GB).
    - `docker compose config` validates.
  </behavior>
  <verify><automated>docker compose -f deploy/docker-compose.yml config</automated></verify>
  <done>FOUND-01 infrastructure present.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke script</name>
  <files>scripts/smoke.sh</files>
  <behavior>
    - Check Temporal UI (8080 or documented port), Grafana (/api/health), Prometheus (/-/healthy), Loki (/ready).
    - Retry with backoff; clear error messages.
    - Executable bit set in git.
  </behavior>
  <verify><automated>./scripts/smoke.sh</automated></verify>
  <done>FOUND-02 satisfied.</done>
</task>

<task type="auto">
  <name>Task 4: CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <behavior>
    - JDK 21, cache Gradle.
    - Steps: build, test, compose config validate.
  </behavior>
  <verify><automated>act or push to branch</automated></verify>
  <done>CI gate established.</done>
</task>

</tasks>

<execution_notes>
After completion, create `01-VERIFICATION.md` and update `.planning/STATE.md` to Phase 2.
</execution_notes>
