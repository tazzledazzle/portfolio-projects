---
phase: 04-instrumentation
plan: 04-01
type: execute
wave: 1
depends_on: [03-ai-workflows]
requirements: [OTEL-01, OTEL-02]
must_haves:
  truths:
    - "Activity execution produces an OTel span with workflow_id, run_id, workflow_type, and activity_type attributes (verified by unit test)."
    - "GET http://localhost:9464/metrics returns Prometheus text including activity_duration and workflow_completed series after one ping workflow."
    - "Worker shutdown flushes OTLP without hanging (SIGTERM hook calls OpenTelemetryConfig.shutdown())."
  artifacts:
    - path: worker/src/main/kotlin/com/portfolio/temporalobs/worker/telemetry/OpenTelemetryConfig.kt
      provides: Global SDK, OTLP + Prometheus exporters, resource attributes
    - path: worker/src/main/kotlin/com/portfolio/temporalobs/worker/telemetry/TemporalTracingInterceptor.kt
      provides: WorkerInterceptor for activity + workflow completion metrics
    - path: worker/src/main/kotlin/com/portfolio/temporalobs/worker/telemetry/Metrics.kt
      provides: Named instruments per ADR-004 cardinality rules
    - path: worker/src/test/kotlin/com/portfolio/temporalobs/worker/telemetry/TemporalTracingInterceptorTest.kt
      provides: InMemorySpanExporter coverage for OTEL-01
---

<objective>
**Wave 1 — Traces and metrics:** Bootstrap OpenTelemetry on the worker, register Temporal interceptors, expose Prometheus metrics, and prove span attributes with automated tests.

Satisfies **OTEL-01** and **OTEL-02**. Logging (OTEL-03) and OkHttp propagation (OTEL-04) are **04-02**.
</objective>

<context>
@.planning/phases/04-instrumentation/04-CONTEXT.md
@.planning/phases/04-instrumentation/04-RESEARCH.md
@.planning/REQUIREMENTS.md
@docs/adr/0002-kotlin-worker-opentelemetry.md
@docs/adr/0004-workflow-trace-correlation.md
@worker/src/main/kotlin/com/portfolio/temporalobs/worker/WorkerMain.kt
@worker/build.gradle.kts
@deploy/prometheus/prometheus.yml
@.env.example
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Gradle OTel dependencies + version resource</name>
  <files>worker/build.gradle.kts, build.gradle.kts (if shared constants needed)</files>
  <read_first>04-RESEARCH.md technology table</read_first>
  <behavior>
    - Add `implementation(platform("io.opentelemetry:opentelemetry-bom:1.43.0"))` and SDK modules: `opentelemetry-api`, `opentelemetry-sdk`, `opentelemetry-exporter-otlp`, `opentelemetry-exporter-prometheus` (alpha OK per research).
    - Add test deps: `opentelemetry-sdk-testing` for `InMemorySpanExporter`.
    - Pass `project.version` into `OpenTelemetryConfig` as `service.version` resource attribute.
  </behavior>
  <verify>
    <automated>./gradlew :worker:compileKotlin :worker:compileTestKotlin</automated>
  </verify>
  <done>Worker module compiles with OTel on classpath.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: OpenTelemetryConfig bootstrap</name>
  <files>worker/src/main/kotlin/com/portfolio/temporalobs/worker/telemetry/OpenTelemetryConfig.kt</files>
  <behavior>
    - `init()` builds `OpenTelemetrySdk` with:
      - `Resource` attributes: `service.name` from `OTEL_SERVICE_NAME` (default `ai-temporal-worker`), `service.version` from Gradle version.
      - `BatchSpanProcessor` + `OtlpGrpcSpanExporter` when `OTEL_TRACES_EXPORTER` != `none`, endpoint from `OTEL_EXPORTER_OTLP_ENDPOINT` (strip trailing slash; default `http://localhost:4317`).
      - `SdkMeterProvider` with `PeriodicMetricReader` + `PrometheusHttpServer` bound to `METRICS_PORT` (default `9464`), path `/metrics`.
    - Expose `val openTelemetry: OpenTelemetry` and `fun shutdown()` (close Prometheus server, shutdown SDK).
    - Register as JVM singleton; safe to call `init()` once from `WorkerMain` before `factory.start()`.
  </behavior>
  <verify>
    <automated>./gradlew :worker:test --tests "*OpenTelemetryConfig*" -q || ./gradlew :worker:compileKotlin</automated>
  </verify>
  <done>SDK starts and exposes metrics port without starting Temporal.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Metrics instruments</name>
  <files>worker/src/main/kotlin/com/portfolio/temporalobs/worker/telemetry/Metrics.kt</files>
  <behavior>
    - Obtain meter from `OpenTelemetryConfig.openTelemetry.getMeter("ai-temporal-worker")`.
    - Define:
      - `activity.duration` — `DoubleHistogram`, unit `s`, attributes: `workflow_type`, `activity_type`, `status` (`ok` / `error`).
      - `workflow.completed` — `LongCounter`, attributes: `workflow_type`, `status`.
      - `llm.request.duration` — `DoubleHistogram`, unit `s`, attributes: `status` (used in 04-02).
    - Helper methods: `recordActivityDuration(...)`, `incrementWorkflowCompleted(...)`, `recordLlmDuration(...)`.
    - **Never** attach `workflow_id` or `run_id` to metric attributes.
  </behavior>
  <verify>
    <automated>./gradlew :worker:compileKotlin</automated>
  </verify>
  <done>Instruments registered at SDK init.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Temporal WorkerInterceptor (activity + workflow)</name>
  <files>worker/src/main/kotlin/com/portfolio/temporalobs/worker/telemetry/TemporalTracingInterceptor.kt, worker/src/main/kotlin/com/portfolio/temporalobs/worker/WorkerMain.kt</files>
  <read_first>Temporal SDK `WorkerInterceptor`, `ActivityInboundCallsInterceptorBase`, `WorkflowInboundCallsInterceptorBase`</read_first>
  <behavior>
    - `TemporalTracingInterceptor` implements `WorkerInterceptor`:
      - **Activity:** wrap `execute`; create span named `activity.${activityType}`; set attributes `workflow_id`, `run_id`, `workflow_type`, `activity_type`, `task_queue` from `ActivityExecutionContext.getInfo()`; record duration in `finally`; set span status on exception.
      - **Workflow:** wrap workflow run; on terminal outcome increment `workflow.completed` with `workflow_type` + `status` (do not create high-cardinality workflow spans unless cheap — completion counter is required for OTEL-02).
    - Wire in `WorkerMain`: `factory.newWorker(TaskQueues.AI_WORKFLOWS, WorkerOptions.newBuilder().setWorkerInterceptors(TemporalTracingInterceptor(...)).build())`.
    - Extend shutdown hook: call `OpenTelemetryConfig.shutdown()` after `factory.awaitTermination`.
  </behavior>
  <verify>
    <automated>./gradlew :worker:test --tests "*TemporalTracingInterceptorTest*" -q</automated>
  </verify>
  <acceptance_criteria>Test exports one span with all four Temporal identifier attributes after executing a test activity.</acceptance_criteria>
  <done>OTEL-01 satisfied; worker runs with interceptor registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Unit tests — spans and metrics</name>
  <files>worker/src/test/kotlin/com/portfolio/temporalobs/worker/telemetry/TemporalTracingInterceptorTest.kt</files>
  <behavior>
    - Use `InMemorySpanExporter` + test `TracerProvider` wired into interceptor under test.
    - Run a minimal activity (e.g. `PingActivitiesImpl.ping`) inside `TestActivityEnvironment` **or** invoke inner interceptor with mocked `ActivityExecutionContext` if simpler.
    - Assert span attributes: `workflow_id`, `run_id`, `workflow_type`, `activity_type` present.
    - Assert `activity.duration` histogram records at least one point (use `InMemoryMetricReader` or OTel test utilities).
  </behavior>
  <verify>
    <automated>./gradlew :worker:test -q</automated>
  </verify>
  <done>CI-green automated proof for OTEL-01/02 instrumentation code paths.</done>
</task>

</tasks>

<verification>
## Wave 1 manual checks (executor)

```bash
# Terminal A — stack (if not up)
docker compose -f deploy/docker-compose.yml up -d temporal otel-collector

# Terminal B — worker
export OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
./gradlew :worker:run

# Terminal C — metrics + workflow
curl -s localhost:9464/metrics | grep -E 'activity_duration|workflow_completed'
./gradlew :starter:run --args="ping"
```

Expect non-empty metric lines after `ping` completes.
</verification>

<success_criteria>
- `./gradlew :worker:test` passes.
- `:9464/metrics` scrapeable; Prometheus config `ai-worker` target works when worker runs on host.
- Spans export to collector `debug` exporter logs when collector is up (full Jaeger proof deferred to Phase 5).
</success_criteria>
