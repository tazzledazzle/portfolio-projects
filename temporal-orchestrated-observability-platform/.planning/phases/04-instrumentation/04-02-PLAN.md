---
phase: 04-instrumentation
plan: 04-02
type: execute
wave: 2
depends_on: [04-01]
requirements: [OTEL-03, OTEL-04]
must_haves:
  truths:
    - "Worker stdout emits one JSON object per log line containing trace_id, workflow_id, and run_id during activity execution."
    - "LLM stub HTTP request includes W3C traceparent and creates a child span parented to the activity span."
    - "scripts/otel-smoke.sh (or documented equivalent) passes in CI-free local verification."
  artifacts:
    - path: worker/src/main/resources/logback.xml
      provides: JSON console appender with MDC fields
    - path: worker/src/main/kotlin/com/portfolio/temporalobs/worker/clients/LlmClient.kt
      provides: OkHttp instrumented client
    - path: worker/src/test/kotlin/com/portfolio/temporalobs/worker/clients/LlmClientTracingTest.kt
      provides: OTEL-04 automated check
    - path: scripts/otel-smoke.sh
      provides: metrics + log spot-check after ping/rag
    - path: .env.example
      provides: METRICS_PORT, OTEL_SERVICE_NAME documented
---

<objective>
**Wave 2 — Logs and outbound HTTP:** JSON structured logging correlated with traces, OkHttp instrumentation for the LLM stub, env documentation, and a small smoke script.

Satisfies **OTEL-03** and **OTEL-04**. Depends on **04-01** SDK and activity interceptor (MDC population).
</objective>

<context>
@.planning/phases/04-instrumentation/04-01-PLAN.md
@worker/src/main/resources/logback.xml
@worker/src/main/kotlin/com/portfolio/temporalobs/worker/clients/LlmClient.kt
@worker/src/main/kotlin/com/portfolio/temporalobs/worker/telemetry/TemporalTracingInterceptor.kt
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: JSON logback + MDC fields</name>
  <files>worker/src/main/resources/logback.xml, worker/build.gradle.kts, worker/.../telemetry/TemporalTracingInterceptor.kt</files>
  <behavior>
    - Add `net.logstash.logback:logstash-logback-encoder` (8.x).
    - Replace plain pattern encoder with `LoggingEventCompositeJsonEncoder` to stdout.
    - Include JSON fields: `timestamp`, `level`, `logger`, `message`, and MDC keys: `trace_id`, `span_id`, `workflow_id`, `run_id`, `workflow_type`, `activity_type`.
    - In activity interceptor: before activity body, populate MDC from `ActivityInfo` + `Span.current().spanContext` (`trace_id` as 32-char hex); `MDC.clear()` in `finally`.
    - Keep log messages short; no prompt text in logs (PITFALLS).
  </behavior>
  <verify>
    <automated>./gradlew :worker:compileKotlin</automated>
  </verify>
  <acceptance_criteria>Running ping prints a single-line JSON log containing `"trace_id"` and `"workflow_id"`.</acceptance_criteria>
  <done>OTEL-03 logging path complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OkHttp OpenTelemetry instrumentation</name>
  <files>worker/src/main/kotlin/com/portfolio/temporalobs/worker/clients/LlmClient.kt, worker/build.gradle.kts, worker/.../telemetry/Metrics.kt</files>
  <read_first>OpenTelemetry OkHttp 3.0 instrumentation docs</read_first>
  <behavior>
    - Add `io.opentelemetry.instrumentation:opentelemetry-okhttp-3.0` (version aligned with OTel BOM 1.43 / instrumentation 2.10+).
    - Build `OkHttpClient` with `OpenTelemetryOkHttpInterceptor` using tracer name `llm-client`.
    - Span name `llm.complete` (not full URL); record `llm.request.duration` in interceptor listener or wrapper.
    - Ensure client is created **after** `OpenTelemetryConfig.init()` — inject client factory from `WorkerMain` or lazy singleton.
    - Update `RagActivitiesImpl` / `AgentActivitiesImpl` to use instrumented client (constructor injection preferred for tests).
  </behavior>
  <verify>
    <automated>./gradlew :worker:test --tests "*LlmClientTracingTest*" -q</automated>
  </verify>
  <done>OTEL-04 satisfied; traceparent sent to stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LlmClientTracingTest</name>
  <files>worker/src/test/kotlin/com/portfolio/temporalobs/worker/clients/LlmClientTracingTest.kt</files>
  <behavior>
    - Start `MockWebServer` returning minimal OpenAI-shaped JSON (reuse stub response shape from WireMock mappings).
    - Run `LlmClient.complete` inside span scope or via activity test harness.
    - Assert recorded span named `llm.complete` (or instrumentation default) exists.
    - Assert request header `traceparent` is non-empty.
  </behavior>
  <verify>
    <automated>./gradlew :worker:test -q</automated>
  </verify>
  <done>Automated OTEL-04 regression guard.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Env + otel smoke script</name>
  <files>.env.example, scripts/otel-smoke.sh, AGENTS.md (short telemetry section if missing)</files>
  <behavior>
    - Document: `OTEL_SERVICE_NAME`, `METRICS_PORT=9464`, `OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_TRACES_EXPORTER`.
    - `scripts/otel-smoke.sh`: fail fast if `:9464/metrics` unreachable; grep for `activity_duration`; optional note to run starter `ping` when worker already running.
    - Do not require Docker Jaeger for Phase 4 pass.
  </behavior>
  <verify>
    <automated>shellcheck scripts/otel-smoke.sh 2>/dev/null || bash -n scripts/otel-smoke.sh</automated>
  </verify>
  <done>Operator quick-check documented.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Phase 4 verification doc</name>
  <files>.planning/phases/04-instrumentation/04-VERIFICATION.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <behavior>
    - After execution, write `04-VERIFICATION.md` with evidence: test output, sample JSON log line (redacted), metrics curl snippet.
    - Mark OTEL-01..04 Done in REQUIREMENTS traceability table.
    - Set STATE.md current phase → 5.
  </behavior>
  <verify>
    <manual>Review 04-VERIFICATION.md checklist all [x]</manual>
  </verify>
  <done>Phase 4 closed for GSD verifier.</done>
</task>

</tasks>

<verification>
## Wave 2 manual checks

```bash
export OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
export LLM_STUB_URL=http://localhost:8090
docker compose -f deploy/docker-compose.yml up -d temporal llm-stub otel-collector
./gradlew :worker:run
# separate shell
./gradlew :starter:run --args="rag What is observability?"
./scripts/otel-smoke.sh
```

Pick one JSON log line and confirm `trace_id` matches a span in collector debug output (or wait for Phase 5 Jaeger).
</verification>

<success_criteria>
- `./gradlew :worker:test` passes including tracing tests.
- JSON logs correlate with active span during RAG activity chain.
- OkHttp requests to LLM stub carry W3C propagation.
</success_criteria>
