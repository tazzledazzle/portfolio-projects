---
phase: 02-temporal
plan: 02-01
type: execute
wave: 1
depends_on:
  - 01-foundation
requirements:
  - TEMP-01
  - TEMP-02
  - TEMP-03
must_haves:
  truths:
    - "Worker logs `Worker started` and polls `ai-workflows` without connection errors."
    - "PingWorkflow completes; Temporal UI shows status Completed."
    - "SIGTERM on worker stops polling within 30s without task loss (documented)."
  artifacts:
    - path: worker/src/main/kotlin/.../WorkerMain.kt
    - path: workflows/src/main/kotlin/.../PingWorkflow.kt
    - path: starter/src/main/kotlin/.../StarterMain.kt
---

<objective>
Prove **Temporal Engine** integration: worker polls, workflow executes, UI visibility.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: PingWorkflow + activity</name>
  <files>workflows/..., worker/...</files>
  <behavior>
    - `PingWorkflow` interface + implementation returning constant string.
    - `PingActivity` logs and returns `pong`.
    - Register on worker factory.
  </behavior>
  <verify><automated>./gradlew test --tests "*Ping*"</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Worker bootstrap</name>
  <files>worker/.../WorkerMain.kt</files>
  <behavior>
    - Build `WorkflowClient` from env.
    - `WorkerFactory.newInstance` poll `ai-workflows`.
    - Shutdown hook on SIGTERM.
  </behavior>
  <verify><manual>Run worker + starter; check Temporal UI</manual></verify>
</task>

<task type="auto">
  <name>Task 3: Starter CLI</name>
  <files>starter/...</files>
  <behavior>
    - `ping` subcommand starts workflow with id `ping-{uuid}`.
    - Prints result and workflow id to stdout.
  </behavior>
  <verify><manual>./gradlew :starter:run --args="ping"</manual></verify>
</task>

<task type="auto">
  <name>Task 4: Docs — Temporal local dev</name>
  <files>docs/LOCAL-DEV.md</files>
  <behavior>
    - Start order: compose → worker → starter.
    - Temporal UI URL and screenshot placeholder OK.
  </behavior>
</task>

</tasks>
