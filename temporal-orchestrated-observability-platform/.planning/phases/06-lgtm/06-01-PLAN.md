---
phase: 06-lgtm
plan: 06-01
type: execute
depends_on: [05-backends]
requirements: [LGTM-01, LGTM-02, LGTM-03, LGTM-04]
must_haves:
  truths:
    - "Grafana Explore Loki query `{service_name=\"ai-temporal-worker\"} |= \"workflow_id\"` returns lines after rag run."
    - "Clicking trace_id in log line opens Tempo trace."
    - "Workflow Overview dashboard shows non-zero workflow.completed rate after load."
    - "docs/OPERATIONS.md lists Grafana URL and default dashboard paths."
---

<objective>
Complete **LGTM Stack - Grafana** layer: unified visualization and log-trace correlation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Tempo OTLP ingest</name>
  <files>deploy/tempo/tempo.yaml, deploy/otel-collector/config.yaml</files>
  <behavior>Collector pipelines traces to tempo:4317; verify in Tempo metrics.</behavior>
</task>

<task type="auto">
  <name>Task 2: Promtail → Loki</name>
  <files>deploy/promtail/config.yml</files>
  <behavior>Parse JSON logs; labels service_name, level; avoid high-cardinality labels.</behavior>
</task>

<task type="auto">
  <name>Task 3: Grafana provisioning</name>
  <files>deploy/grafana/provisioning/datasources/, deploy/grafana/provisioning/dashboards/</files>
  <behavior>Prometheus, Loki, Tempo datasources; UID stable for links.</behavior>
</task>

<task type="auto">
  <name>Task 4: Dashboards</name>
  <files>deploy/grafana/dashboards/workflow-overview.json, activity-latency.json, llm-proxy.json</files>
  <behavior>
    - Overview: rate(workflow_completed), error ratio.
    - Latency: histogram_quantile on activity_duration.
    - LLM: llm_request_duration, stub OK.
  </behavior>
</task>

<task type="auto">
  <name>Task 5: Load sample + screenshot doc</name>
  <files>docs/GRAFANA.md</files>
  <behavior>Script runs 3 workflows; documents panel expectations for portfolio README.</behavior>
</task>

</tasks>
