---
phase: 07-operations
plan: 07-01
type: execute
depends_on: [06-lgtm]
requirements: [OPS-01, OPS-02, OPS-03]
must_haves:
  truths:
    - "OPERATIONS.md has executable steps for all four failure modes."
    - "promtool check rules passes on alerts.yml."
    - "07-VERIFICATION.md documents manual execution of each runbook with PASS/FAIL."
---

<objective>
Deliver **Operations / On-Call** readiness: runbooks, alerts, verified procedures.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: OPERATIONS.md</name>
  <files>docs/OPERATIONS.md</files>
  <behavior>
    Sections: Stuck workflow (Temporal terminate/reset), Missing traces (collector/SDK checklist), Prometheus target down, Loki/Tempo disk pressure.
    Include correlation_id / workflow_id grep examples.
  </behavior>
</task>

<task type="auto">
  <name>Task 2: Alert rules</name>
  <files>deploy/prometheus/alerts.yml</files>
  <behavior>
    - HighActivityFailureRate: rate(activity failures) > threshold.
    - OtelExportErrors: increase if metric exposed.
    - PrometheusTargetDown: up==0 for ai-worker.
  </behavior>
  <verify><automated>promtool check rules deploy/prometheus/alerts.yml</automated></verify>
</task>

<task type="auto">
  <name>Task 3: Manual validation</name>
  <files>.planning/phases/07-operations/07-VERIFICATION.md</files>
  <behavior>Execute each runbook once; record date, steps, outcome.</behavior>
</task>

<task type="auto">
  <name>Task 4: Portfolio README polish</name>
  <files>README.md</files>
  <behavior>Architecture diagram reference, demo GIF placeholder, link OPERATIONS + GRAFANA docs.</behavior>
</task>

</tasks>
