---
phase: 03-ai-workflows
plan: 03-01
type: execute
depends_on: [02-temporal]
requirements: [WF-01, WF-02, WF-03, WF-04]
must_haves:
  truths:
    - "Each of rag, agent, batch starter commands completes with workflow result printed."
    - "Agent workflow retry visible in Temporal UI when failure simulation enabled."
    - "Batch workflow child activities count matches N in aggregate output."
---

<objective>
Implement the **AI Workflows** layer from the diagram: three reference patterns on Temporal with stubs.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: LLM + retrieval stubs</name>
  <files>deploy/docker-compose.yml, worker/.../clients/</files>
  <behavior>WireMock or minimal HTTP stub; OkHttp client without OTel yet.</behavior>
</task>

<task type="auto">
  <name>Task 2: RagQaWorkflow</name>
  <files>workflows/.../RagQaWorkflow.kt, worker/.../activities/RagActivities.kt</files>
  <behavior>Three activities in sequence; return answer string with citation path.</behavior>
  <verify><automated>integration test with TestWorkflowEnvironment</automated></verify>
</task>

<task type="auto">
  <name>Task 3: AgentToolsWorkflow</name>
  <files>workflows/.../AgentToolsWorkflow.kt</files>
  <behavior>RetryPolicy max 3 on callTool; synthesize after successful tool.</behavior>
</task>

<task type="auto">
  <name>Task 4: BatchEvalWorkflow</name>
  <files>workflows/.../BatchEvalWorkflow.kt</files>
  <behavior>Async activity fan-out; aggregate mean score.</behavior>
</task>

<task type="auto">
  <name>Task 5: Starter commands + tests</name>
  <files>starter/..., worker/src/test/...</files>
  <behavior>CLI args rag|agent|batch; CI uses TestWorkflowEnvironment only.</behavior>
</task>

</tasks>
