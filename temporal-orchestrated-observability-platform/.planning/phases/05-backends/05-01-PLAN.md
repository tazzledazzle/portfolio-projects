---
phase: 05-backends
plan: 05-01
type: execute
depends_on: [04-instrumentation]
requirements: [BACK-01, BACK-02, BACK-03]
must_haves:
  truths:
    - "Jaeger UI search by service `ai-temporal-worker` returns trace with ≥3 spans for rag workflow."
    - "Prometheus target `ai-worker` state UP after sample run."
    - "CI job runs `otelcol validate --config=deploy/otel-collector/config.yaml` successfully."
---

<objective>
Wire **Jaeger** and **Prometheus** to the OpenTelemetry SDK — the diagram's trace and metrics export path.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: OTel Collector config</name>
  <files>deploy/otel-collector/config.yaml</files>
  <behavior>OTLP gRPC 4317, export to Jaeger OTLP endpoint, prometheus exporter for collector metrics.</behavior>
</task>

<task type="auto">
  <name>Task 2: Jaeger in Compose</name>
  <files>deploy/docker-compose.yml</files>
  <behavior>jaeger service; UI port 16686 documented.</behavior>
</task>

<task type="auto">
  <name>Task 3: Prometheus scrape config</name>
  <files>deploy/prometheus/prometheus.yml</files>
  <behavior>Scrape worker metrics path; 15s interval.</behavior>
</task>

<task type="auto">
  <name>Task 4: Integration script</name>
  <files>scripts/trace-smoke.sh</files>
  <behavior>Start rag workflow; sleep; curl Jaeger API for recent trace; fail if empty.</behavior>
</task>

<task type="auto">
  <name>Task 5: CI collector validate</name>
  <files>.github/workflows/ci.yml</files>
  <behavior>Add otelcol validate step using collector image.</behavior>
</task>

</tasks>
